* Finish the Wiki basic data accessors (load, store, exists).

* Once we know where the data is stored, we have to know data format to
  read/write and get the internal memory structure WikiParser
  (WikiParserTxt2tag, WikiParselWakka, WikiParserXML, etc.)

* Clean up accessors as storage_dependant modules (WikiStorageFile,
  WikiStorageSqlite, etc.).

* Create a generic WikiGui (WikiGuiGtk, WikiGuiWeb, etc.)

